FROM ubuntu
RUN apt-get update -qq && apt-get install -y nodejs

ENV APP_HOME /usr/local/cors-anywhere

RUN mkdir $APP_HOME
WORKDIR $APP_HOME

COPY ./cors-anywhere/ $APP_HOME

ENV CORSANYWHERE_WHITELIST https://trustedsite.com,http://evilsite.com
ENV PORT 8080

CMD ["node", "server.js"]
